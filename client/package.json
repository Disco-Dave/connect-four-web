{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "serve": "rm -rf .parcel/serve && parcel --cache-dir .parcel/cache -d .parcel/serve static/index.html --open",
    "bundle": "rm -rf dist && parcel build --no-source-maps --cache-dir .parcel/cache -d dist static/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm-hot": "^1.1.5",
    "node-elm-compiler": "^5.0.5",
    "parcel-bundler": "^1.12.4"
  }
}
